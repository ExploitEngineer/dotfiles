# VA-API variables removed after the 2026-08-29 GPU fault. See the Graphics
# section of the dotfiles README. NVD_BACKEND=direct in particular bypasses EGL
# and is the least-tested path in nvidia-vaapi-driver.
#
# Wayland hints are unrelated to the fault and are kept.
MOZ_ENABLE_WAYLAND=1
ELECTRON_OZONE_PLATFORM_HINT=auto
